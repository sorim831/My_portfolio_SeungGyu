<?xml version="1.0" encoding="UTF-8"?>
    <manifest xmlns:tools="http://schemas.android.com/tools"
        package="com.example.classtest" xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:theme="@style/Theme.Classtest" android:supportsRtl="true" android:roundIcon="@mipmap/ic_launcher_round" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:allowBackup="true">
    <activity android:name=".MainActivity"
        android:exported="true"
        tools:ignore="Instantiatable">
    <intent-filter>
    <action android:name="android.intent.action.MAIN"/>
    <category android:name="android.intent.category.LAUNCHER"/>
</intent-filter>
</activity>
        <activity android:name=".HelpActivity" android:label="SecondActivity"></activity>
        <activity android:name=".SecondActivity" android:label="SecondActivity"></activity>
        <activity android:name=".ThirdActivity" android:label="SecondActivity"></activity>
        <service
            android:name=".MusicActivity" />
</application>
</manifest>